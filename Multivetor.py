import collections

class Multivetor:
	multivetor

	def __init__():
		multivetor = collections.OrderedDict()

	def inserirBase(base: int, escala: long):
		if (escala != 0):
			multivetor[base] = escala
		else:
			del multivetor[base]

	def removerBase(base: int):
		inserirBase(base, 0)